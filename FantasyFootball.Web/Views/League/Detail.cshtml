@model FantasyFootball.Service.Fantasy.Models.League
@{
    ViewBag.Title = "League";
}

<h2>@Model.Name</h2>
<ul>
    @foreach(var team in Model.Teams){
    <li>@Html.ActionLink(team.Name,"Detail","Team",new {id=team.Id})</li>
    }
</ul>
