@model IEnumerable<FantasyFootball.Service.Fantasy.Models.League>
@{
    ViewBag.Title = "Leagues";
}

<ul>
    @foreach(var league in Model){
        <li>@Html.ActionLink(league.Name,"Detail",new {id=league.Id})</li>
    }
</ul>
